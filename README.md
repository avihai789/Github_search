# Github_search
A website that you can search users in github and it saves the 10 most searched users.

To run it you must have MySQL and Apache, and run them via XAMPP.
you can search for users in github an it saves the 10 most searched users.
you can also delete the saved searches.
